@model IEnumerable<EBook.Models.LoaiSachModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenTheLoai)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenTheLoai)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LoaiID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LoaiID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LoaiID })
        </td>
    </tr>
}

</table>
